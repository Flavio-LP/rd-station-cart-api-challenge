version: '3.8'

services:
  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
  
  redis:
    image: redis:7.0.15-alpine
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
  
  web:
    build: .
    command: sh -c "rm -f tmp/pids/server.pid && bundle exec rails db:prepare && bundle exec rails server -b 0.0.0.0"
    volumes:
      - .:/rails
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/myapp_development
      - REDIS_URL=redis://redis:6379/0
      - RAILS_ENV=development
    user: root
  
  sidekiq:
    build: .
    command: bundle exec sidekiq -C config/sidekiq.yml
    volumes:
      - .:/rails
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/myapp_development
      - REDIS_URL=redis://redis:6379/0
      - RAILS_ENV=development
    user: root
  
  test:
    build: .
    command: |
      sh -c "
      sleep 5 &&
      echo '🔧 Preparando ambiente de teste...' &&
      bundle exec rails db:drop RAILS_ENV=test &&
      bundle exec rails db:prepare RAILS_ENV=test &&
      echo '' &&
      echo '📝 Gerando documentação Swagger...' &&
      bundle exec rspec spec/integration --format Rswag::Specs::SwaggerFormatter --order defined &&
      echo '✅ Documentação Swagger gerada em swagger/v1/swagger.yaml' &&
      echo '' &&
      echo '🧪 Executando bateria de testes...' &&
      bundle exec rspec --format json --out tmp/rspec_results.json --format documentation &&
      ruby -rjson -e '
        results = JSON.parse(File.read(\"tmp/rspec_results.json\"))
        total = results[\"summary\"][\"example_count\"]
        failures = results[\"summary\"][\"failure_count\"]
        pending = results[\"summary\"][\"pending_count\"]
        success = total - failures - pending
        duration = results[\"summary\"][\"duration\"]
        
        puts \"\"
        puts \"╔═══════════════════════════════════════════════════════════════╗\"
        puts \"║                    📊 RESUMO DOS TESTES                       ║\"
        puts \"╠═══════════════════════════════════════════════════════════════╣\"
        puts \"║  ✅ Testes com SUCESSO: #{success.to_s.rjust(3)} / #{total}#{\" \" * (28 - total.to_s.length)}║\"
        puts \"║  ❌ Testes com ERRO:    #{failures.to_s.rjust(3)} / #{total}#{\" \" * (28 - total.to_s.length)}║\"
        puts \"║  ⏭️  Testes PENDENTES:   #{pending.to_s.rjust(3)} / #{total}#{\" \" * (28 - total.to_s.length)}║\"
        puts \"║  ⏱️  Tempo de execução: #{duration.round(2)}s#{\" \" * (30 - duration.round(2).to_s.length)}║\"
        puts \"╠═══════════════════════════════════════════════════════════════╣\"
        
        if failures > 0
          puts \"║  ❌ TESTES QUE FALHARAM:#{\" \" * 38}║\"
          puts \"╠═══════════════════════════════════════════════════════════════╣\"
          
          results[\"examples\"].each do |example|
            if example[\"status\"] == \"failed\"
              description = example[\"full_description\"]
              file_path = example[\"file_path\"]
              line_number = example[\"line_number\"]
              exception = example[\"exception\"]
              
              if description.length > 59
                description = description[0..55] + \"...\"
              end
              
              puts \"║  📍 #{description}#{\" \" * (59 - description.length)}║\"
              
              location = \"#{file_path}:#{line_number}\"
              if location.length > 59
                location = \"...\" + location[-56..-1]
              end
              puts \"║     📂 #{location}#{\" \" * (56 - location.length)}║\"
              
              if exception
                error_class = exception[\"class\"] || \"Error\"
                error_message = exception[\"message\"] || \"\"
                
                if error_class.length > 59
                  error_class = error_class[0..55] + \"...\"
                end
                puts \"║     ⚠️  #{error_class}#{\" \" * (57 - error_class.length)}║\"
                
                error_lines = error_message.split(\"\\n\")
                error_lines.first(3).each do |line|
                  line = line.strip
                  if line.length > 59
                    line = line[0..55] + \"...\"
                  end
                  puts \"║        #{line}#{\" \" * (59 - line.length)}║\"
                end
              end
              
              puts \"╠═══════════════════════════════════════════════════════════╣\"
            end
          end
        end
        
        if failures == 0 && pending == 0
          puts \"║  🎉 TODOS OS TESTES PASSARAM! 🎉#{\" \" * 28}║\"
        elsif failures == 0
          puts \"║  ✅ Todos os testes executados passaram!#{\" \" * 20}║\"
          puts \"║  ⏭️  Existem #{pending} testes pendentes de implementação#{\" \" * (26 - pending.to_s.length)}║\"
        else
          puts \"║  ⚠️  #{failures} teste(s) falharam - veja detalhes acima#{\" \" * (25 - failures.to_s.length)}║\"
        end
        
        puts \"╚═══════════════════════════════════════════════════════════════╝\"
        puts \"\"
        
        if failures > 0
          puts \"💡 Dica: Execute os testes individualmente para mais detalhes:\"
          results[\"examples\"].each do |example|
            if example[\"status\"] == \"failed\"
              puts \"   bundle exec rspec #{example[\"file_path\"]}:#{example[\"line_number\"]}\"
            end
          end
          puts \"\"
        end
        
        exit(failures > 0 ? 1 : 0)
      '
      "
    volumes:
      - .:/rails
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/myapp_test
      - REDIS_URL=redis://redis:6379/0
      - RAILS_ENV=test
    user: root

volumes:
  postgres_data:
  redis_data: